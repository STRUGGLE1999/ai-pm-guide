# AGENTS.md

## 发布与资料文件规则

- 平时先用本地预览确认，不频繁发生产。
- 要发线上时再集中一次生产部署。
- 大 PDF 不自动加载，只放“打开 / 下载”链接。
- 以后资料多了，大文件可以放 GitHub Release 或别的存储，网站只放索引链接。
- 以后如果涉及需要 Git LFS 上传的大文件，不代为上传；只给出终端命令，让用户在本机终端执行，避免长时间上传消耗 token。

## 站内配图风格

- 以后生成的站内插图风格都按照 `baoyu-article-illustrator` 技能里的 `hand-drawn-edu` preset，不需要每次单独提醒。
- `hand-drawn-edu` 对应 `infographic + sketch-notes + macaron`：暖米色纸张背景、黑色手绘线条、马卡龙淡彩圆角模块、少量珊瑚红强调、简洁教学图标，整体像课堂板书/知识卡片。
- 插图必须按该技能流程生成 PNG 位图，不用 SVG、HTML、Canvas 或代码图替代。
- 图片中文字只放少量大号关键词，避免长句和密集标注；如果生成结果出现乱码、错字或不可读，重新按技能流程生成，不做代码覆盖修补。
- 单张图控制信息密度，优先表达流程、结构和学习顺序，不做装饰性插画。
